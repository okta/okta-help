<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
    <body>
        <p>Template: Caution. Convert this snippet to text (right-click/ctl-click for menu). Then delete these instructions, click into the grey area, and start typing. <br />IMPORTANT: If you copy in content, ensure the paragraph style retains the <code>&lt;p&gt;</code> class <code style="font-weight: bold;">noteContent</code>, or reset it. Other <code>&lt;p&gt;</code> tags must also use the <code>noteContent</code> class.</p>
        <div class="note_cautionOkta">
            <p class="noteContent">Replace this text, keep paragraph style (<span class="code">class="noteContent"</span>)</p>
        </div>
    </body>
</html>