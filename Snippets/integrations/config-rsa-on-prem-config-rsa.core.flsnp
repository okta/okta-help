<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
    <body>
        <ol>
            <li>Select the <span class="wintitle">Factor Types</span> tab.</li>
            <li>Select the <span class="uicontrol">RSA SecurID</span> factor, and click <span class="uicontrol">Edit</span>.</li>
            <li>If prompted, click <span class="uicontrol">Enable RSA&#160;SecurID</span>, then click <span class="uicontrol">Edit</span>.</li>
            <li>Enter the following fields:                  <ul><li><span class="uicontrol">RSA username format</span>: Select the format expected by the provider.<br /></li><li><span class="uicontrol">Hostname</span>: The server host name or IP address.</li><li><span class="uicontrol">Authentication Port</span>: The RADIUS server port (for example, 1812). This is defined when the On-Prem RADIUS server is configured.</li><li><span class="uicontrol">Shared Secret</span>: An authentication key that must be defined when the RADIUS server is configured, and must be the same on both the RADIUS client and server.</li></ul></li>
            <li>Click  <span class="uicontrol">Add New Agent</span>. Note the value of the instance ID. You're also provided with a download link for the  agent installer.<br /></li>
            <li><span class="uicontrol">Enable</span> or <span class="uicontrol">Disable</span> as required.</li>
            <li> Click <span class="uicontrol">Save</span>.</li>
        </ol>
    </body>
</html>