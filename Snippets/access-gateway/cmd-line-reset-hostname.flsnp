<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
    <body>
        <div>
            <div class="noteOkta">
                <p class="noteContent">Set the hostname of the appliance to a different name than the Access Gateway hostname. The hostname <span class="userinput">admin</span> is reserved for the admin UI, and can't be used as the appliance hostname.</p>
            </div>
            <ol>
                <li>
                    <MadCap:snippetBlock src="oag-cmd-line-console-signin.flsnp" />
                </li>
                <li>Enter <span class="userinput">5 - System</span>.<br /></li>
                <li>Enter <span class="userinput">1 - Change Hostname</span>.</li>
                <li>At the <span class="uicontrol">Enter new hostname:</span> prompt, enter the replacement hostname.</li>
                <li>Enter <span class="userinput">Y</span> to continue, or <span class="userinput">N</span> to cancel.</li>
                <li>Enter <span class="userinput">X</span> to return to the system menu.</li>
            </ol>
        </div>
    </body>
</html>