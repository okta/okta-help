<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
    <head>
        <link href="" rel="stylesheet" type="text/css" />
    </head>
    <body>
        <div>
            <p>Increasing the number of threads the <MadCap:variable name="okta-feature-names.AD agent short" /> uses to poll the server for tasks allows the existing <MadCap:variable name="okta-feature-names.AD agent short" /> to process more requests without the need to install additional <MadCap:variable name="okta-feature-names.AD agent short" />s.</p>
            <ol>
                <li>Open Windows Services and stop the <MadCap:variable name="okta-feature-names.AD agent short" /> service.</li>
                <li>From the terminal, locate the&#160;<span class="code">OktaAgentService.exe.instances.config</span>&#160;file for each <MadCap:variable name="okta-feature-names.AD agent short" /> server:&#160;<p><span class="code">C:\Program Files (x86)\Okta\Okta AD Agent\OktaAgentService.exe.config</span></p></li>
                <li>
                    <p>Open the 'OktaAgentService.exe.config' file in a text editor  and then locate this entry:</p>
                    <p><code>&lt;add key="PollingThreads" value="2" /&gt;</code>
                    </p>
                    <p>The default value is 2 and the maximum value is 10.</p>
                </li>
                <li>
                    <p>Save the file and then restart the <MadCap:variable name="okta-feature-names.AD agent short" /> service. To verify that the setting has changed, open the agent.log file at startup and observe the startup information at the bottom of the file:</p>
                    <p><span class="code">2017/07/21 06:06:22.167 Debug – TEST-SERVER-1(4) – PollingThreads: &lt;thread number&gt;</span></p>
                </li>
            </ol>
        </div>
    </body>
</html>